function theBeatlesPlay(musicians, instruments) {
  const arr = [];

  for ( let i = 0; i = musicians.length; i++) {
    arr.push(`${musicans[i]}`) + arr.push(`${instruments[i]}`)

    return arr
  }
}